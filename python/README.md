# Running the app

```
$ python main.py
```

# Running the tests

```
$ python -m venv .venv
# or
$ python3 -m venv .venv
$ source .venv/bin/activate
# or
$ .venv\Scripts\activate.bat
$ pip install -r requirements.txt
$ pytest
```
